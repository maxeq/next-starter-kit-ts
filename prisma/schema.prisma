generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model dyna {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  date        String
  description String
  keyword     String
  title       String
}
